---
import { getI18N } from "@i18n/index";
import CookiesR from "./CookiesR";

const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
const { body, button } = i18n.Cookies;
---

<CookiesR client:load body={body} button={button} />
